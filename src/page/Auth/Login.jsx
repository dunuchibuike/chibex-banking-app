import React, { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import "./Login.css";
import { FaRegEye, FaRegEyeSlash, FaUser, FaLock, FaFingerprint, FaShieldAlt } from "react-icons/fa";
import axios from "axios";
import { BaseURL } from "../../lib/HighFunction";
export const AuthScene = () => <aside className="auth_scene"><div className="brand">CHIBEX<span> / PRIVATE</span></div><div className="scene_copy"><span className="eyebrow">Private banking, reimagined</span><div className="security_orb"><FaShieldAlt /></div><h2>Your money,<br />in clear view.</h2><p>Thoughtfully secure financial tools for the life you are building.</p></div><div className="network-status"><i />All systems secure · 256-bit encryption</div></aside>;
const Login = () => { const [showPassword,setShowPassword]=useState(false),[loading,setLoading]=useState(false); const navigate=useNavigate(); const [userInfo,setUserInfo]=useState({emailAddress:"",password:""}); const [errorMsg,setErrorMsg]=useState({err:false,name:"",msg:""}); const update=(field,value)=>{setUserInfo({...userInfo,[field]:value}); if(!value.trim())setErrorMsg({err:true,name:field==="emailAddress"?"email":"password",msg:`Enter your ${field==="emailAddress"?"email":"password"}`});else setErrorMsg({err:false,name:"",msg:""})}; const getTokenUserId=(token)=>{try{const payload=JSON.parse(atob(token.split(".")[1]));return payload.id||payload.userId||payload._id||""}catch{return ""}}; const handleLogin=async(e)=>{e.preventDefault();if(!userInfo.emailAddress.trim()||!userInfo.password.trim()){setErrorMsg({err:true,name:"general",msg:"Enter your email and password"});return}setLoading(true);try{const res=await axios.post(`${BaseURL}/login`,{...userInfo,emailAddress:userInfo.emailAddress.trim().toLowerCase(),password:userInfo.password.trim()});if(res.status>=200&&res.status<300&&res.data?.token){const user=res.data.user||{},id=user._id||getTokenUserId(res.data.token);localStorage.setItem("token",res.data.token);if(id)localStorage.setItem("Id",id);navigate(user.fullName?`/dashboard/${user.fullName}`:"/dashboard");alert(res.data.message||"Login successful")}else throw new Error("The login response was incomplete")}catch(error){setErrorMsg({err:true,name:"general",msg:error.response?.data?.message||error.message||"Login failed"})}finally{setLoading(false)}}; return <div className="login_container"><AuthScene /><main className="auth_panel"><section className="login_card"><header className="login_header"><span className="eyebrow">Welcome back</span><h1>Access your account</h1><p>Your dashboard is protected with bank-grade security.</p></header><form className="login_form" onSubmit={handleLogin}><div className="form_group"><label htmlFor="email">Email address</label><div className="input_wrapper"><FaUser className="input_icon"/><input type="email" id="email" placeholder="you@example.com" value={userInfo.emailAddress} onChange={e=>update("emailAddress",e.target.value)} autoComplete="email"/></div><span className="field_error">{errorMsg.name==="email"?errorMsg.msg:""}</span></div><div className="form_group"><label htmlFor="password">Password</label><div className="input_wrapper password_wrapper"><FaLock className="input_icon"/><input type={showPassword?"text":"password"} id="password" placeholder="Enter your password" value={userInfo.password} onChange={e=>update("password",e.target.value)} autoComplete="current-password"/><button type="button" className="toggle_password" aria-label={showPassword?"Hide password":"Show password"} onClick={()=>setShowPassword(!showPassword)}>{showPassword?<FaRegEyeSlash/>:<FaRegEye/>}</button></div><span className="field_error">{errorMsg.name==="password"?errorMsg.msg:""}</span></div>{errorMsg.name==="general"&&<span className="error_text">{errorMsg.msg}</span>}<div className="remember_forgot"><label className="remember_me"><input type="checkbox"/><span>Keep me signed in</span></label><Link to="/forgot-password" className="forgot_link">Forgot password?</Link></div><button type="submit" className={`login_btn ${loading?"loading":""}`} disabled={loading}>{loading?"Unlocking account…":"Continue securely"}</button></form><div className="signup_link"><p><FaFingerprint style={{marginRight:6,verticalAlign:"-2px"}}/>Biometric sign-in is available on supported devices.</p><p style={{marginTop:14}}>New to Chibex? <Link to="/signup">Create an account</Link></p></div></section></main></div>};
export default Login;
